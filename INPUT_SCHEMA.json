{
  "title": "India Stock Fundamentals Scraper",
  "description": "Get public Indian stock prices, ratios, financial statements, and shareholding data from Screener.in and Moneycontrol. No login or API key required.",
  "type": "object",
  "schemaVersion": 1,
  "required": ["symbols"],
  "properties": {
    "symbols": {
      "title": "NSE symbols or BSE codes",
      "description": "Examples: RELIANCE, TCS, INFY, HDFCBANK, or 500325. Start with one symbol, then scale up.",
      "type": "array",
      "editor": "stringList",
      "default": ["RELIANCE"],
      "uniqueItems": true,
      "items": { "type": "string" }
    },
    "source": {
      "title": "Data source",
      "description": "Use Screener.in, Moneycontrol, or merge both into one record per stock.",
      "type": "string",
      "editor": "select",
      "enum": ["both", "screener", "moneycontrol"],
      "enumTitles": ["Both sources", "Screener.in only", "Moneycontrol only"],
      "default": "both"
    },
    "consolidated": {
      "title": "Use consolidated financials",
      "description": "Use consolidated Screener.in results when available.",
      "type": "boolean",
      "default": true,
      "editor": "checkbox"
    },
    "includeFinancials": {
      "title": "Include quarterly and annual results",
      "description": "Adds the latest four quarters and five annual periods from Screener.in. Keep off for the cheapest first test.",
      "type": "boolean",
      "default": false,
      "editor": "checkbox"
    },
    "includeShareholding": {
      "title": "Include shareholding pattern",
      "description": "Adds the latest promoter, FII, DII, and public holdings from Screener.in when available.",
      "type": "boolean",
      "default": false,
      "editor": "checkbox"
    },
    "maxResults": {
      "title": "Maximum stocks",
      "description": "Limits the number of unique symbols processed in one run. Increase after confirming the first result.",
      "type": "integer",
      "minimum": 1,
      "maximum": 100,
      "default": 1,
      "editor": "number"
    },
    "maxConcurrency": {
      "title": "Concurrency",
      "description": "Number of stocks processed at the same time. Keep this modest to avoid rate limits.",
      "type": "integer",
      "minimum": 1,
      "maximum": 10,
      "default": 1,
      "editor": "number"
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "description": "Optional proxy settings. Datacenter or no proxy usually works; residential proxy can improve reliability at scale.",
      "type": "object",
      "editor": "proxy",
      "default": {
        "useApifyProxy": false
      }
    }
  }
}
