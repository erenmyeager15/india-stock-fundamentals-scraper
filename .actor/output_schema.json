{
  "actorOutputSchemaVersion": 1,
  "title": "Moneycontrol + Screener Comparison Output",
  "description": "Normalized Indian stock records with source availability, side-by-side values, percentage differences, lakh/crore warnings, source observation timestamps, fiscal-period limitations, and agreement scores.",
  "type": "object",
  "properties": {
    "results": {
      "type": "string",
      "title": "Dual-source stock comparison records",
      "description": "One normalized dataset item per stock, including source-level values and comparison results when both sources are available.",
      "template": "{{links.apiDefaultDatasetUrl}}/items"
    }
  }
}
